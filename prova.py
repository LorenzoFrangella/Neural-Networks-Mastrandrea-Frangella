import numpy as np
import torch

# NumPy array
arr_torch_expanded = arr_torch[..., None]  # Adds a new axis at the endprint(arr_np[...,None],arr_np)arr_torch_expanded = arr_torch[..., None]  # Adds a new axis at the end